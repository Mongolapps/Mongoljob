АЖИЛ ЗУУЧЛАХ TELEGRAM БОТ V2

1. ZIP файлыг задлана.
2. Termux дээр:
   cd /sdcard/Download/job_match_bot_v2
3. Суулгах:
   pip install -r requirements.txt
4. Тохиргоо:
   cp .env.example .env
   nano .env
5. .env файлд BOT_TOKEN болон ADMIN_ID оруулна.
6. Асаах:
   python bot.py

АДМИН КОМАНД
/pending — хүлээгдэж буй зар
/stats — статистик
/cancel — үйлдэл цуцлах
